import os
from dotenv import load_dotenv

# Загружаем переменные окружения из файла .env
load_dotenv()

# Токен бота Telegram
TELEGRAM_BOT_TOKEN = os.getenv('TELEGRAM_BOT_TOKEN')

# ID Google таблицы
SPREADSHEET_ID = os.getenv('SPREADSHEET_ID')

# ID чата мастера для уведомлений
MASTER_CHAT_ID = os.getenv('MASTER_CHAT_ID')

# Путь к файлу авторизации Google
CREDENTIALS_FILE = 'credentials.json'

# Названия колонок в таблице (обновлено: добавлен ID записи)
COLUMNS = {
    'booking_id': 'ID записи',
    'timestamp': 'Время записи',
    'name': 'Имя',
    'phone': 'Телефон',
    'date': 'Дата',
    'time': 'Время',
    'service': 'Услуга',
    'telegram_id': 'Telegram ID',
    'username': 'Username',
    'status': 'Статус',
    'status_updated': 'Время изменения статуса',
    'reschedule_id': 'ID переноса',
    'original_booking_id': 'ID исходной записи'
}

# Уникальные цвета для статусов (улучшена цветовая схема)
STATUS_COLORS = {
    'ожидает': {'red': 1.0, 'green': 1.0, 'blue': 0.85},        # 🌕 Светло-желтый
    'подтверждено': {'red': 0.85, 'green': 1.0, 'blue': 0.85},  # ✅ Светло-зеленый
    'выполнено': {'red': 0.85, 'green': 0.95, 'blue': 1.0},     # 💙 Голубой
    'отменено': {'red': 0.95, 'green': 0.95, 'blue': 0.95},     # ⬜ Серый
    'отклонено': {'red': 1.0, 'green': 0.9, 'blue': 0.9},       # ❌ Светло-красный
    'запрос переноса': {'red': 1.0, 'green': 0.85, 'blue': 0.7},# 🔄 Оранжевый
    'предложение переноса': {'red': 0.9, 'green': 0.95, 'blue': 1.0}, # 📨 Светло-синий
}

# Упрощенные статусы
STATUSES = {
    'PENDING': 'ожидает',
    'CONFIRMED': 'подтверждено',
    'COMPLETED': 'выполнено',
    'CANCELLED': 'отменено',
    'REJECTED': 'отклонено',
    'RESCHEDULE_REQUESTED': 'запрос переноса',
    'RESCHEDULE_OFFERED': 'предложение переноса',
}

# Коды статусов для мастер-панели
MASTER_STATUSES = {
    'active': 'подтверждено',
    'completed': 'выполнено',
    'pending': 'ожидает',
    'reschedule': 'запрос переноса',
}

# Приоритеты сортировки по статусам (чем меньше число, тем выше приоритет)
STATUS_PRIORITY = {
    'ожидает': 1,
    'подтверждено': 2,
    'запрос переноса': 3,
    'предложение переноса': 4,
    'выполнено': 5,
    'отменено': 6,
    'отклонено': 7,
}